import TotClient from "../../components/TotClient";

export const metadata = {
  title: "ToT 便利贴",
  description: "To Tyycc 的碎碎念便利贴。"
};

export default function TotPage() {
  return <TotClient />;
}
